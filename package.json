{
  "name": "react-artists",
  "version": "1.0.0",
  "description": "a demo MEARN application about music artists",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "cd ./client-artists && npm run start",
    "server": "cd ./server-artists && npm run server",
    "dev": "npm run server & npm run client"
  },
  "author": "guillaume@owlab",
  "license": "ISC",
  "dependencies": {
    "cookie-parser": "^1.4.4",
    "jsonwebtoken": "^8.5.1"
  }
}
